PeerOrgs:
$(for ((i=1; i<=3; i++)); do
    ORG_NAME="Org${i}"
    echo "  - Name: ${ORG_NAME}"
    echo "    Domain: ${ORG_NAME,,}.example.com"
    echo "    EnableNodeOUs: true"
    echo "    Template:"
    echo "      Count: 1"
    echo "    Users:"
    echo "      Count: 1"
done)
OrdererOrgs:
  - Name: Orderer
    Domain: example.com
    Specs:
      - Hostname: orderer1
